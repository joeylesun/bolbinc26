suvos_backend_single.py: The main application logic (Camera, YOLO, WebSocket Server, Serial Control).

SUVOS_Monitor.spec: The build configuration file for PyInstaller (defines how to pack the app).

interface/: Contains the HTML frontend dashboard.

yolov8n.pt: The YOLO model weights.

calibration_data_cam0.npz: Camera calibration matrix.

led_zones.json & room_shape.json: Configuration files for zones and room geometry.
